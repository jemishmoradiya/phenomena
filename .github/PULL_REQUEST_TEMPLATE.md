## What changed

## Checklist

- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] If this touches a simulation/animation: verified visually in the browser (`npm run dev`), not just via a passing build
- [ ] If this addresses a `plans/` backlog item, its Boundaries section was followed
